%h1 Create Employee of Workplace
-if params[:employee_id]
	%h4= "#{@employee.em_id}: #{@employee.name}"
- elsif params[:workplace_id]
	%h4= "#{@workplace.name}"
- else
- if params[:employee_id]
	= link_to "Cancel", employee_path(@employee), class: "btn btn-danger"
- elsif params[:workplace_id]
	= link_to "Cancel", workplace_path(@workplace), class: "btn btn-danger"
%br
%br
= render 'form'

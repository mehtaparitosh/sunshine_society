.card
  .card-body
    .form.container
      = simple_form_for(@program) do |f|
        = f.error_notification
        .form-inputs
          .row
            .col
              = f.input :name
            .col
              = f.input :full_form
        .row
          .col
            = f.input :comments
        .form-actions
          .row
            .col
            .col
              = f.button :submit, class: "btn btn-submit btn-lg"
            .col
